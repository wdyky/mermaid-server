/**
 * Server version. Bump manually — shown by `--version`, in `/api/health`,
 * and in the startup log line. A constant on purpose: package.json is not
 * readable at runtime in the bundled single-file build.
 */
export const VERSION = '0.1.0'
